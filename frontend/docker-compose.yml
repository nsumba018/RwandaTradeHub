version: '3.8'

services:
  rwandatrade-hub:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: rwandatrade-hub
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
